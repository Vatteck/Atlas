# .last-agent — integrity checkpoint for the next AI coding agent

**Agent:** Claude Opus 5 — 2026-08-01
**Branch:** master
**Last commit (base):** 2efbe53 ("docs: bump STATUS.md version to 0.16.1")

## Summary

Vatteck returned after a ~2-week gap and asked for a project-state review plus a direction.
Review found **no drift** — docs matched the tree, 774 Python + 60 JS tests green, CI green,
0.16.1 live on the AUR, the BACKLOG feature menu drained, and the 5-task AUR security
heuristics plan fully shipped.

Direction chosen: **0.17 — "verify and show," not "add."** Rationale is in the STATUS.md
decision log. This session did step 1 (debt cut), docs only:

- **Split STATUS.md** (2,379 lines / 94 KB — it no longer fit an agent's read budget) into a
  215-line live baton + new **`docs/HISTORY.md`** (Done log, retired gotchas, Rust/Qt-era
  decision log). AGENTS.md §1 now says HISTORY.md is *not* required reading.
- **Fixed a real doc bug:** the large-files gotcha named `view/core/controller.py` at "~192 KB";
  it is actually **32 KB**. The 220 KB file is `gems/arch/controller.py`, and the two largest
  files (`main.js` 340 KB, `api.py` 184 KB) were unlisted. Corrected in STATUS.md + AGENTS.md §8.
- Deleted two fully-merged dead local branches (`feat/webview-polish-sprint-1`, `-2`).
- Removed ~120 MB of regenerable `makepkg` artifacts under `linux_dist/arch/` (src/, pkg/,
  built `.pkg.tar.zst`, tarballs). Both `.SRCINFO` files and all tracked sources untouched.

**Verification:** 774 Python + 60 JS tests pass after the change; `git status` shows only the
intended doc edits. No app code touched.

Then, same day, with Vatteck's go-ahead:

- **GUI eyeball CLEARED.** Vatteck walked 0.16.1's PKGBUILD inline reader, the boot splash, and
  the theme-preset/accent contrast on the real desktop — all good. **Nothing is currently
  awaiting a GUI eyeball.**
- Added a **bug-report issue template** (`.github/ISSUE_TEMPLATE/`) that front-loads the debug
  log + `atlas --self-check` output.
- **Fixed the GitHub repo description** (it led with "AppImage, Arch/AUR, Flatpak, Snap, Web" —
  burying Arch, advertising three off-by-default sources).
- **Deleted three fully-merged remote branches** (`feat/webview-polish-sprint-2`, two
  `claude/*`), each re-confirmed at 0 commits ahead of master first. `origin` is now
  master-only.

**Next agent:** STATUS.md "Next" #2 — re-shoot the five `docs/screenshots/*.png`, which are from
2026-06-02 and predate themes/accents, the floating terminal, the dep tree, and the calmed
`.pacnew` center. Needs a GUI session, so it is Vatteck's. Then #3: a render-level test harness
for the `main.js` view renderers (where every recent defect actually lived and the suite is
blind), or the paused cold-start memory thread.
