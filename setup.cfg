[metadata]
description_file = README.md

[tool:pytest]
# Only collect the real test tree. Without this, a makepkg build under
# linux_dist/arch/release/src/ leaves a full repo copy (incl. tests/) that pytest would
# otherwise discover as duplicate modules and fail collection.
testpaths = tests
